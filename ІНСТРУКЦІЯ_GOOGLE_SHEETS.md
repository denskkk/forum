# 📊 Налаштування Google Sheets для форми

## Крок 1: Створіть Google Таблицю

1. Відкрийте https://sheets.google.com
2. Створіть нову таблицю
3. Назвіть її: **"Реєстрація на форум"**

## Крок 2: Додайте заголовки

У першому рядку додайте такі заголовки (точно в такому порядку):

| A1 | B1 | C1 | D1 | E1 | F1 |
|---|---|---|---|---|---|
| Дата та час | ПІБ | Посада | Організація | Область/місто | Email |

| G1 | H1 | I1 | J1 | K1 | L1 | M1 |
|---|---|---|---|---|---|---|
| Телефон | Питання спікеру? | Текст питання | Потреби доступності | Інші потреби | Згода на обробку даних | Коментарі |

## Крок 3: Створіть Google Apps Script

1. У вашій таблиці натисніть **Розширення** → **Apps Script**
2. Видаліть весь код у редакторі
3. Скопіюйте та вставте цей код:

```javascript
function doPost(e) {
  try {
    // Отримуємо активну таблицю
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    
    // Парсимо дані
    var data = JSON.parse(e.postData.contents);
    
    // Додаємо новий рядок з даними
    sheet.appendRow([
      data.timestamp,
      data.fullName,
      data.position,
      data.organization,
      data.location,
      data.email,
      data.phone,
      data.hasQuestion,
      data.question,
      data.accessibility,
      data.accessibilityOther,
      data.consent,
      data.comments
    ]);
    
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'success',
      'message': 'Дані успішно збережено'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'error',
      'message': error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}
```

4. Натисніть **Зберегти** (іконка дискети)
5. Назвіть проект: **"Форма Реєстрації"**

## Крок 4: Розгорніть веб-додаток

1. Натисніть **Розгорнути** → **Нове розгортання**
2. Натисніть іконку шестерні ⚙️ → Виберіть **Веб-додаток**
3. Налаштування:
   - **Опис**: "API для форми реєстрації"
   - **Виконати як**: **Я** (Me)
   - **Хто має доступ**: **Будь-хто** (Anyone)
4. Натисніть **Розгорнути**
5. **ДУЖЕ ВАЖЛИВО**: Скопіюйте URL - він виглядає так:
   ```
   https://script.google.com/macros/s/ВАШІ_СИМВОЛИ_ТУТ/exec
   ```

## Крок 5: Авторизація

При першому розгортанні Google попросить дозвіл:

1. Натисніть **Авторизувати доступ**
2. Виберіть свій Google-акаунт
3. Натисніть **Додатково** (Advanced)
4. Натисніть **Перейти до [назва проекту]** (Go to...)
5. Натисніть **Дозволити** (Allow)

## Крок 6: Підключіть URL до форми

1. Відкрийте файл `script.js`
2. Знайдіть рядок:
   ```javascript
   const GOOGLE_SCRIPT_URL = 'ВСТАВЬТЕ_СЮДА_ВАШ_URL_ИЗ_APPS_SCRIPT';
   ```
3. Замініть на ваш скопійований URL:
   ```javascript
   const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/ВАШІ_СИМВОЛИ/exec';
   ```
4. Збережіть файл

## Крок 7: Тестування

1. Відкрийте `index.html` у браузері
2. Заповніть форму тестовими даними
3. Натисніть "Зареєструватися"
4. Перевірте Google Таблицю - там має з'явитися новий рядок з даними

## ✅ Готово!

Тепер всі відповіді з форми будуть автоматично зберігатися у вашій Google Таблиці!

---

## Вирішення проблем

### Помилка "Script function not found: doPost"
- Переконайтеся, що ви скопіювали весь код
- Перевірте, що функція називається саме `doPost`

### Дані не з'являються в таблиці
- Перевірте, що URL правильно скопійований у `script.js`
- Перевірте, що ви дали всі дозволи при авторизації
- Подивіться в консоль браузера (F12) на наявність помилок

### Помилка авторизації
- Спробуйте створити нове розгортання
- Переконайтеся, що вибрали "Будь-хто" (Anyone) у налаштуваннях доступу
